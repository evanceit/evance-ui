import {SelectStrategy} from "../select-strategies.ts";

/**
 * # Single Any
 *
 * One node may be selected, regardless of whether the node has children.
 *
 * @param mandatory
 */
export const singleAny = (mandatory?: boolean): SelectStrategy => {

};