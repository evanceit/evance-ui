import { Meta } from '@storybook/blocks';
import EvanceUiLogo from './assets/evance-ui-logo.svg?url';
import { version } from '../../.storybook/version';

<Meta title="Introduction" />

<img src={EvanceUiLogo} width="120" />

Version <code>{version}</code>

# Evance UI Library &amp; Design System

Evance UI is a Vue.js component library created by Azexis for the Evance platform and for future embedded apps.

```sh
npm i @evance/evance-ui
```
